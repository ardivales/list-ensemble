{
  "name": "ensemble-campagne",
  "version": "1.0.0",
  "private": true,
  "description": "Site de campagne — Liste Ensemble, élections des délégués Webb Fontaine 2026",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./assets/tailwind.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./assets/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
